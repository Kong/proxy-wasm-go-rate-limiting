_format_version: "1.1"
_transform: true

services:
- name: demo
  url: http://mockbin.org
  routes:
  - name: my-route
    paths:
    - /
    strip_path: false
    plugins:
    - name: proxy-wasm
      config:
        filters:
        - name: rate-limiting
          config: '{"minute": 3}'